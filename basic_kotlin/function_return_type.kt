package basic_kotlin

// function yang mengembalikan data --> maka harus menulisakan tipe data pengembalian data tersebut secara eksplisit åå
fun hitungLebar(tinggi: Int , lebar : Int): Int{
    var total = tinggi * lebar
    return total
}